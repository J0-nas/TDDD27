from flask import Flask, request
from gevent.wsgi import WSGIServer

app = Flask(__name__)

@app.route('/'):
def hello():
    app.send_static_file('index.html')

if __name__ == '__main__':
    WSGIServer(('', 5000), {'/test':app})
